{
    "database": {
        "username": "{{DATABASE_USERNAME}}",
        "password": "{{DATABASE_PASSWORD}}",
        "name": "{{DATABASE_NAME}}",
        "host": "{{DATABASE_HOST}}",
        "port": "{{DATABASE_PORT}}",
        "sslmode": "disable"
    },
    "log": {
        "level": "TRACE"
    }
}